<% annotated = @document.annotated_word_ids %>
<div id="extract-content" class="col-xs-12 annotatable" data-annotate="word" data-source-document-id="<%= @source_document.id %>" data-metadata-group-id="<%= @group.id %>">
	<% @document.content.css("text p").each do |paragraph| %>
		<div class="paragraph">
			<% paragraph.css("s").each do |sentence| %>
				<div class="sentence">
					<% sentence.css("w").each do |word| %>
						<% annotation = annotated[word["id"]] if annotated.has_key?(word["id"]) %>
						<div id="<%= word["id"] %>" class="word <%= "no-space" if word["pws"].eql?("no") %> <%= "annotated" if annotation %>" <%= "data-entity-mention-id=#{annotation}" if annotation %>><%= word.content.html_safe %></div>
					<% end %>
				</div>
			<% end %>
		</div>
	<% end %>
</div>
